requests[socks]>=2.31.0
twilio>=8.0.0
rich>=13.0.0
py-clob-client>=0.18.0
eth-account>=0.11.0
websockets>=12.0
websocket-client>=1.7.0         # real-time Binance WebSocket feed (adapters/binance_ws.py)
aiohttp>=3.9.0
fastapi>=0.110.0
uvicorn>=0.29.0
krakenex>=2.1.0
PySocks>=1.7.1
slowapi>=0.1.9
cryptography>=46.0.0
passlib[argon2,bcrypt]>=1.7.4
openai>=1.30.0
bcrypt>=4.0.0
python-multipart>=0.0.9
httpx>=0.27.0
psycopg[binary,pool]>=3.2.0
redis>=5.0.0
rq>=1.16.0

# ── NLP / Sentiment ──────────────────────────────────────────────────────────
vaderSentiment>=3.3.2          # local sentiment scoring (services/vader_sentiment.py)

# ── Optional: strategy optimisation ─────────────────────────────────────────
optuna>=3.6.0                  # weight optimisation for signal aggregator (optional)

# ── Optional: enhanced technical analysis ───────────────────────────────────
# pandas>=2.2.0                # enable if consuming DataFrame-based TA pipelines
# pandas-ta>=0.3.14b           # 130+ TA indicators; drop-in complement to services/technical_indicators.py

# ── Optional: ML / advanced modeling ─────────────────────────────────────────
# scikit-learn>=1.5.0          # ML models for signal classification
# xgboost>=2.0.0               # gradient boosting for bet outcome prediction
# lightgbm>=4.3.0              # fast alternative to XGBoost

# ── Optional: async HTTP (for parallel adapter polling) ──────────────────────
# aiohttp>=3.9.0               # already in requirements — use for async adapter polling

# ── Scheduling (optional — for periodic maintenance tasks) ───────────────────
# apscheduler>=3.10.0          # cron-like scheduling for db_hygiene, health checks
